import { StubPage } from "@/components/stub-page";

export default function MethodsPage() {
  return (
    <StubPage
      eyebrow="Methods / Static route"
      title="Correctness is the product."
      summary="The final methods page will document exact arithmetic, independent oracles, and reproducible simulations."
    />
  );
}
